const Sequelize = require('sequelize');
const db = require('../config/database');

const Track = db.define(['require', 'dependency'], function(require, factory) {
  'use strict';

});

module.exports = Track;
